create table Processor(
    id integer,
    model varchar(255),
    cores integer,
    threads integer,
    maxfrequency varchar(255),
    cache varchar(255),
    maxram varchar(255),
    ramtype varchar(255),
    ramchannels integer,
    primary key (id)
);
